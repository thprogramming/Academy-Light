# كيف تضيفي لعبة جديدة للموقع

الموقع بالكامل عبارة عن ملفات ثابتة (HTML) — ما في سيرفر، ما في قاعدة بيانات، ما في تسجيل دخول. إضافة لعبة = نسخ ملفات + تعديل نص + رفع على GitHub.

---

## الخطوة 1: انسخي مجلد القالب

انسخي مجلد `templates/game-template` بالكامل، وحطي النسخة جوا مجلد `games/`، وسمّيها باسم اللعبة بالإنجليزي وبدون مسافات (مثال: `space-race`).

النتيجة المطلوبة:
```
games/
└── space-race/            ← اللعبة الجديدة (نسخة القالب بعد التسمية)
```

---

## الخطوة 2: جهّزي الملفات داخل المجلد الجديد

حطي بمجلد اللعبة الجديدة:

| الملف | شو هو | إجباري؟ |
|---|---|---|
| `cover.svg` أو `cover.png` أو `cover.jpg` | صورة غلاف اللعبة | نعم |
| `programmer.svg` أو `programmer.png` أو `programmer.jpg` | صورة المبرمج | نعم |
| `screenshot-1.png`, `screenshot-2.png` | لقطات من اللعبة | مستحسن |
| `play.html` | نسخة اللعبة المصدّرة من TurboWarp (تشتغل بالمتصفح) | اختياري لكن مستحسن جداً |
| `game.apk` | ملف تثبيت أندرويد | اختياري |

**ملاحظة:** لو رفعتي صورة بصيغة `.png` أو `.jpg` بدل `.svg`، لازم تغيّري امتداد الملف في `index.html` كمان.

---

## الخطوة 3: عدّلي ملف index.html تبع اللعبة

افتحي `index.html` جوا مجلد اللعبة الجديدة بأي محرر نصوص (حتى Notepad أو محرر GitHub مباشرة أونلاين)، ودوّري عالنصوص المكتوبة بين قوسين مربعين `[...]` وبدّليها بمعلومات اللعبة الحقيقية:

- `[اسم اللعبة]` — بيتكرر بأكتر من مكان، بدّليه بنفس الاسم بكل مرة
- `[تاريخ النشر — مثال: 10 سبتمبر 2026]`
- `[اسم المبرمج]`
- `[العمر]`
- `[اكتبي هون وصف مختصر...]`
- النقاط الثلاث تحت "عن اللعبة" — عدّليها أو احذفي أي وحدة مش لازمة

لو غيّرتي امتداد الصور (من `.svg` لـ `.png` أو `.jpg` مثلاً)، دوّري بالملف عن `cover.svg` و`programmer.svg` و`screenshot-1.svg` و`screenshot-2.svg` وبدّليهم بالاسم الصح.

---

## الخطوة 4: ضيفي بطاقة اللعبة بالصفحة الرئيسية

افتحي `index.html` (اللي بالمجلد الرئيسي، مش تبع اللعبة). رح تلاقي داخل الملف تعليق واضح مكتوب:

```
<!-- بطاقة لعبة — انسخي هالبلوك بالكامل... -->
```

انسخي أي بلوك `<article class="game-card...` لحد `</article>`، الصقيه تحت البطاقة الأخيرة، وبدّلي فيه:
- الرابط: `games/calculator/index.html` → `games/space-race/index.html`
- رابط ومصدر الصورة: `games/calculator/cover.png` → `games/space-race/cover.jpg`
- اسم اللعبة، اسم المبرمج، عمره، صورته، والوصف المختصر

---

## الخطوة 5: ارفعي التعديلات على GitHub

من موقع GitHub تبع الريبو، ارفعي مجلد اللعبة الجديدة (سحب وإفلات)، واحفظي التعديل على `index.html` الرئيسي، واضغطي Commit. بعد دقيقة تقريباً الموقع بيتحدّث تلقائياً على GitHub Pages.

---

## نصيحة أخيرة

قبل ما ترفعي، افتحي ملف اللعبة الجديد (`index.html` تبعها) مباشرة من جهازك (دبل كليك عليه) وتأكدي إنه طالع منيح وكل شي مكتوب صح، قبل ما ترفعيه.
